import styled from 'styled-components';
import { CardMedia } from '@material-ui/core';

const ExtCardMedia = styled(CardMedia)`
  && {
    flex-direction: row-reverse;
  }
`;

export default ExtCardMedia;
